push.firebaseKey=${FIREBASE_KEY:skyr}
push.firebaseUrl=${FIREBASE_URL:skyr}

server.port=${TEST_PORT:8080}

spring.mail.host=smtp.gmail.com
spring.mail.username=${NOTIF_MAIL_ADDRESS:changeme@gmail.com}
spring.mail.password=${NOTIF_MAIL_PASSWORD:changeme}
spring.mail.port=587

spring.mail.firebaseProperties.mail.smtp.starttls.enable=true
spring.mail.firebaseProperties.mail.smtp.auth=true
spring.mail.firebaseProperties.mail.smtp.starttls.required=true

spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.required=true

# ## Local db filestore: in base directory in folder "data"
spring.datasource.url=jdbc:h2:file:./data/app_db;DB_CLOSE_ON_EXIT=FALSE

# h2 defaults:
spring.datasource.username=sa
spring.datasource.password=

spring.datasource.driverClassName=org.h2.Driver

# Use "create" or "create-drop" when you wish to recreate database on restart; use 
# "update" or "validate" when data is to be kept.
spring.jpa.hibernate.ddl-auto=update

# 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
